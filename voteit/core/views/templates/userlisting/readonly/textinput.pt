<tal:comment tal:replace="None">
  The user is not expected to edit the field, so the value of the field are displayed directly,
  while it is also kept in a hidden field.
</tal:comment>

<span tal:omit-tag="">
    <input type="hidden" name="${field.name}" value="${cstruct}" 
           tal:attributes="size field.widget.size;
                           class field.widget.css_class"
           id="${field.oid}"/>
    <p id="${field.oid}">${cstruct}</p>
    <script tal:condition="field.widget.mask" type="text/javascript">
      deform.addCallback(
         '${field.oid}',
         function (oid) {
            $("#" + oid).mask("${field.widget.mask}", 
                              {placeholder:"${field.widget.mask_placeholder}"});
         });
    </script>
</span>
